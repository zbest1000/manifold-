{
  "name": "topic-canvas",
  "version": "2.0.0",
  "description": "A node-graph explorer for MQTT and OPC UA networks, with an MCP server for AI agents",
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run server:dev\" \"npm run client:dev\"",
    "start": "npm run server:start",
    "server:dev": "cd server && npm run dev",
    "server:start": "cd server && npm start",
    "client:dev": "cd client && npm run dev",
    "client:build": "cd client && npm run build",
    "client:preview": "cd client && npm run preview",
    "mcp": "cd mcp && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../mcp && npm install",
    "build": "npm run client:build",
    "lint": "cd server && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "mqtt",
    "opcua",
    "iot",
    "sparkplug",
    "node-graph",
    "visualization",
    "mcp",
    "network-discovery",
    "real-time"
  ],
  "license": "MIT"
}
