# Demo broker config — anonymous access for easy local testing ONLY.
# Do not expose this to an untrusted network.

listener 1883
protocol mqtt

listener 9001
protocol websockets

allow_anonymous true

# Keep it in-memory; a demo broker doesn't need durable state.
persistence false

# $SYS/# broker stats are published by default and power the "sys" view.
